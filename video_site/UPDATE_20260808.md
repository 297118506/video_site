# 更新说明 - 2026-08-08

## 新增功能：短视频分类 + 短·分类管理

本次更新围绕**短视频分类独立化**和**短视频模块体验优化**展开，核心改动是：为短视频建立独立的分类体系（short_categories），与原有的视频分类（categories）完全隔离，互不干扰。

---

## 一、短视频模块改造

### 1.1 路由变化

| 原路由 | 新路由 | 说明 |
|--------|--------|------|
| `/shorts` (直接播放) | `/shorts` | 分类列表页（先选分类再播放） |
| — | `/shorts/play` | 播放所有短视频（= 点击"全部"） |
| — | `/shorts/play/<category_id>` | 播放指定短分类中的短视频 |

### 1.2 短视频分类列表页 (/shorts)

- 页面顶部大标题「短视频分类」
- 网格卡片布局，自适应列数
- 第一张卡片固定为「全部」橙红渐变配色，显示总视频数量
- 其它分类按"父分类→子分类"顺序展示
- 父分类会统计其子分类的视频总数，并显示「含子分类」标签
- 卡片悬浮上浮效果，视觉体验更好
- 空状态时引导用户「去后台创建分类」或「直接播放全部」

### 1.3 播放页增强 (/shorts/play/<category_id>)

- 顶部栏**左侧**新增：
  - 返回按钮（←）：返回分类列表
  - 当前分类标签（图标 + 名称）
  - 分类下拉选择器：不离开播放页即可切换到其它分类
- 顶部栏**右侧**保持不变：自动切换开关、随机播放开关、进度指示 (1/N)
- 顶部栏布局由 `justify-content: center` 改为 `space-between`

---

## 二、后台管理：短·分类管理

### 2.1 菜单位置

在**所有后台页面**的侧边栏，「分类管理」下方新增「短·分类管理」（图标 `bi-folder-symlink`）：

```
仪表盘
视频管理
短视频管理
分类管理
短·分类管理 ← 新增
系列管理
OpenList管理
用户管理
```

### 2.2 功能特性

与「分类管理」完全一致，但操作的是独立的 `short_categories` 表：

- 添加分类（支持父子层级、排序）
- 列表层级展示（父分类加粗，子分类缩进显示「├─」）
- 单个删除 + 批量删除（带确认提示）
- 分页（每页 20 条）
- 删除分类时会同时删除该分类下的短视频（仅 `is_short_video=1` 的记录，不影响普通视频）

### 2.3 标题说明

短·分类管理页面标题带副标题提示：
> 短·分类管理（仅用于短视频模块，与普通分类互不干扰）

### 2.4 短视频管理页面联动

- 「短视频管理」列表页的分类筛选，下拉选项变为**短分类**
- 「添加短视频」表单的分类选择框，变为**短分类**
- 「编辑短视频」表单同上

---

## 三、OpenList 导入：分类选择联动

### 3.1 自动切换分类源

根据「导入到模块」下拉的选择，自动切换分类下拉的选项：

| 导入到模块 | 分类下拉源 | 标签文字 |
|-----------|-----------|---------|
| 全部视频（和首页） | `categories` 表（原分类管理） | 「导入到分类」 |
| 仅短视频模块 | `short_categories` 表（短·分类管理） | 「导入到短·分类」 |

### 3.2 实现细节

- 页面加载时：注入两个全局变量 `videoCategories` 和 `shortCategories`
- `switchCategorySource()` 函数：根据当前模块值重建 `<select>` 的 `<option>`
- `DOMContentLoaded` 阶段调用一次，保证初始态正确
- 模块 change 事件中再次调用
- 刷新分类按钮 (`refreshCategories()`) 也会根据模块调用对应 API：
  - 全部视频 → `GET /api/categories`
  - 短视频模块 → `GET /api/short_categories`

### 3.3 原有功能保持

- 切换到短视频模块仍然会：
  - 禁用/取消「自动创建系列」复选框
  - 隐藏文件列表中的图片
  - 禁用封面选择

---

## 四、APP 开放 API 新增接口

### 4.1 `/api/v1/short_categories` (新增)

获取短视频分类树，返回分类、每个分类的视频数量统计，以及短视频总数。

```
GET /api/v1/short_categories
```

响应:
```json
{
  "success": true,
  "categories": [
    {
      "id": 1, "name": "搞笑", "parent_id": 0, "sort_order": 0,
      "children": [
        {"id": 3, "name": "街头采访", "parent_id": 1, "children": []}
      ]
    }
  ],
  "total_short_count": 160,
  "category_counts": {"0": 5, "1": 120, "3": 35}
}
```

### 4.2 `/api/v1/shorts` 增强 (category_id 参数)

```
GET /api/v1/shorts?category_id=1&per_page=500
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `category_id` | 0（全部） | 短视频分类 ID，对应短分类表中的 id |
| `page` | 1 | 页码 |
| `per_page` | 20 | 每页数量，最大 500 |
| `q` | - | 搜索关键词（同时匹配 title 和 description） |

### 4.3 全部 V1 API 汇总

| 接口 | 说明 |
|------|------|
| `GET /api/v1/categories` | 普通视频分类树 |
| `GET /api/v1/short_categories` | 短视频分类树 + 数量统计 (新增) |
| `GET /api/v1/videos` | 普通视频列表 |
| `GET /api/v1/videos/<id>` | 视频详情 |
| `GET /api/v1/shorts?category_id=...` | 短视频列表，支持短分类筛选 (增强) |
| `GET /api/v1/series` | 系列列表 |
| `GET /api/v1/series/<id>/episodes` | 系列分集 |
| `GET /api/v1/stats` | 统计数据 |
| `GET /api/v1/search?q=&type=all|video|shorts` | 全局搜索 |

---

## 五、数据库变更

### 5.1 新增表 `short_categories`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| name | TEXT NOT NULL | 分类名称 |
| slug | TEXT UNIQUE | URL 友好标识 |
| parent_id | INTEGER DEFAULT 0 | 父分类 ID (0=顶级) |
| sort_order | INTEGER DEFAULT 0 | 排序权重 |
| created_at | TIMESTAMP | 创建时间 |

与 `categories` 表结构完全相同，但独立存在。应用启动时通过 `CREATE TABLE IF NOT EXISTS` 自动创建，无需手工迁移。

### 5.2 videos 表结构不变

- `category_id` 仍然使用同一列，但语义根据 `is_short_video` 的值自动区分：
  - `is_short_video=1` 的视频 → `category_id` 指向 `short_categories.id`
  - `is_short_video=0` 的视频 → `category_id` 指向 `categories.id`

---

## 六、受影响文件清单

### 后端
- [models.py](file:///D:/文件管理/VIDEO/video_site/models.py)
  - `init_db()` 新增 `short_categories` 表建表 SQL
  - 新增 `get_all_short_categories`, `create_short_category`, `delete_short_category`, `batch_delete_short_categories`, `get_short_category_tree`, `_collect_child_short_category_ids`

- [app.py](file:///D:/文件管理/VIDEO/video_site/app.py)
  - import 增加 `get_db` 及短分类相关函数
  - 新增 `GET /shorts` → `shorts_index()` 分类列表页
  - 新增 `GET /shorts/play`, `GET /shorts/play/<id>` → `shorts_play()` 播放页
  - 新增 `GET /admin/short_categories` → 后台短分类管理
  - 新增 `GET /api/short_categories`, `POST /api/short_categories`, `DELETE /api/short_categories/<id>`
  - 新增 `POST /api/short_categories/batch_delete`
  - `GET /admin/shorts`, `GET /admin/shorts/add`, `GET /admin/shorts/edit/<id>` 改为使用 `short_categories`
  - `GET /admin/openlist` 透传 `short_categories`
  - `GET /api/v1/short_categories` 新增开放接口
  - `GET /api/v1/shorts` 增加 `category_id` 参数支持

### 模板
- [shorts_index.html](file:///D:/文件管理/VIDEO/video_site/templates/shorts_index.html)（新增）
  - 短视频分类列表页，卡片网格布局 + 全部入口 + 子分类展示

- [shorts.html](file:///D:/文件管理/VIDEO/video_site/templates/shorts.html)
  - 顶部栏左侧新增「返回」「当前分类标签」「分类切换下拉」

- [admin/short_categories.html](file:///D:/文件管理/VIDEO/video_site/templates/admin/short_categories.html)（新增）
  - 短分类管理页面，功能同 categories.html，操作 short_categories 表

- [admin/categories.html](file:///D:/文件管理/VIDEO/video_site/templates/admin/categories.html)
  - 侧边栏新增「短·分类管理」菜单项

- [admin/dashboard.html, series.html, series_form.html, openlist.html, users.html, videos.html, video_form.html]
  - 侧边栏同步新增「短·分类管理」菜单项

- [base.html](file:///D:/文件管理/VIDEO/video_site/templates/base.html)
  - 修正导航链接：`shorts_feed` → `shorts_index`

- [admin/openlist.html](file:///D:/文件管理/VIDEO/video_site/templates/admin/openlist.html)
  - 注入 `videoCategories`, `shortCategories` 两个 JS 数组
  - 新增 `setCategoryOptions()`, `switchCategorySource()` 函数
  - `refreshCategories()` 改为根据模块调用不同 API
  - 模块切换事件调用 `switchCategorySource()`
  - 分类 `<label>` 增加 `for="importCategory"` 属性

### 样式
- [style.css](file:///D:/文件管理/VIDEO/video_site/static/css/style.css)
  - `.shorts-top-bar` 改为左右 space-between 布局
  - 新增 `.shorts-left`, `.shorts-back-btn`, `.shorts-cat-label`, `.shorts-cat-switch` 样式

### 文档
- [DEVELOPMENT.md](file:///D:/文件管理/VIDEO/video_site/DEVELOPMENT.md)
  - 同步更新数据库设计、API 接口、APP 开发指南等章节
- [UPDATE_20260808.md](file:///D:/文件管理/VIDEO/video_site/UPDATE_20260808.md)（本文件）
  - 本次更新的完整说明

---

## 七、验证结果

所有路由测试通过：

```
/shorts (分类列表):            200 OK
/shorts/play/0 (播放全部):      200 OK, 160 条视频
/admin/short_categories:       200 OK
/admin/videos:                  200 OK
/admin/shorts:                  200 OK
/admin/openlist:                200 OK (shortCategories 注入成功)
/api/v1/short_categories:      200 OK, success=True
/api/v1/shorts?category_id=0:  200 OK, total=160
```

---

# 更新说明 - 2026-08-10

## 新增功能：OpenList 多选视频导入

**更新日期：2026-08-10**

本次更新优化了 OpenList 管理中的视频导入功能，支持在目录浏览时同时多选多个视频文件一次性导入，无需逐个导入或整目录导入。

---

### 一、功能说明

此前在 OpenList 文件列表中只能：
- 点击「选择此目录」批量导入整个目录下所有视频（会创建系列）
- 点击单个视频导入单个文件

本次新增**多选导入**模式：在目录下有多个视频时，用户可点击视频项自由选择一个或多个视频，再一次性导入。导入的视频使用原文件名作为标题（与单个视频导入逻辑一致），不创建系列。

---

### 二、前端改动（[openlist.html](file:///D:/文件管理/VIDEO/video_site/templates/admin/openlist.html)）

#### 2.1 状态变量调整

将原单个视频选择变量 `selectedVideoFile`（对象）改为数组 `selectedVideoFiles`，支持存储多个选中项：

```javascript
var selectedVideoFiles = [];  // 存储所有选中的视频文件
var importMode = 'directory'; // 'directory' | 'single'
```

每个选中项结构：
```javascript
{ name: 'video.mp4', path: '/current/dir', fullPath: '/full/path/video.mp4' }
```

#### 2.2 视频文件项 UI

每个视频文件项添加复选框，支持两种选择方式：
- **点击整行**：切换选中/取消选中状态
- **点击复选框**：同样切换状态（`event.stopPropagation()` 避免重复触发）

选中状态视觉反馈：
- 整行背景色变为浅绿色 `#d4edda`
- 复选框勾选状态同步更新

#### 2.3 核心函数

| 函数 | 作用 |
|------|------|
| `toggleVideoSelection(filePath, fileName)` | 切换视频选中状态，更新 DOM 高亮和复选框（不重载列表，保持滚动位置） |
| `updateSelectVideoBtn()` | 更新「选择视频」按钮文案：未选时禁用；选中后显示「已选 N 个视频」 |
| `confirmVideoSelection()` | 确认选择，设置 `importMode='single'`，显示已选视频清单 |
| `selectCurrentDir()` | 切换回目录模式时清空 `selectedVideoFiles` |

#### 2.4 批量导入逻辑

`batchImport()` 在 `importMode === 'single'` 时：
1. 过滤出当前目录下选中的视频
2. 弹出确认提示（列出前 4 个视频名 + 总数 + 封面 + 模块）
3. 调用新增的 `POST /api/openlist/multi_import` 接口
4. 展示导入结果（成功/跳过/失败数量 + 详情列表）

---

### 三、后端改动（[app.py](file:///D:/文件管理/VIDEO/video_site/app.py)）

#### 3.1 新增接口 `POST /api/openlist/multi_import`

**请求参数：**
```json
{
  "account": { "server_url": "...", "username": "...", "password": "...", "token": "..." },
  "files": [
    { "file_path": "/path/to/video1.mp4", "file_name": "video1.mp4" },
    { "file_path": "/path/to/video2.mp4", "file_name": "video2.mp4" }
  ],
  "category_id": 1,
  "cover_url": "https://...",
  "is_short_video": 0
}
```

**响应：**
```json
{
  "success": true,
  "imported_count": 2,
  "skipped_count": 0,
  "failed_count": 0,
  "imported": [
    { "name": "video1", "nfo_used": true },
    { "name": "video2", "nfo_used": false }
  ],
  "nfo_used": 1
}
```

#### 3.2 处理流程

对 `files` 数组中的每个文件：
1. 调用 `api.get_file_link()` 获取直链
2. 去重检查：若 `video_url` 已存在则计入 `skipped`
3. 默认使用原文件名作为标题
4. 调用 `_fetch_nfo_for_video()` 尝试解析同目录 NFO 文件：
   - 若存在 NFO，标题/描述/封面/分类/评分/年份/演员等字段优先使用 NFO 内容
   - NFO 封面优先级高于用户指定封面（与单个导入逻辑一致）
5. 调用 `create_video()` 写入数据库
6. 成功计入 `imported`，异常计入 `failed`

#### 3.3 与现有接口的对比

| 接口 | 用途 | 是否创建系列 | 标题来源 |
|------|------|------------|---------|
| `POST /api/openlist/single_import` | 导入单个视频 | 否 | 文件名 / NFO 标题 |
| `POST /api/openlist/multi_import` (新增) | 导入多个选中视频 | 否 | 文件名 / NFO 标题 |
| `POST /api/openlist/batch_import` | 导入整个目录 | 可选（按集数） | 目录名+集数 / NFO 标题 |

---

### 四、保持不变的约束

遵循项目既定规则：
- 多选导入**不创建系列**（仅 `batch_import` 在勾选「自动创建系列」时才会创建）
- 标题优先级：NFO 标题 > 原文件名
- 封面优先级：NFO 封面 > 用户指定封面
- 短视频模块下导入时，文件列表仍隐藏图片、禁用封面选择、禁用自动创建系列
- 已存在的视频（按 `video_url` 去重）自动跳过

---

# 更新说明 - 2026-08-10 (二)

## 优化：「选择此目录」导入时的视频命名逻辑

**更新日期：2026-08-10**

---

### 一、优化背景

此前在 OpenList 管理中点击「选择此目录」批量导入视频时，**无论是否创建合集**，视频标题一律使用「目录名 第N集」格式。这导致用户取消「自动创建系列」后导入的视频仍被命名为"目录名 第1集"等，而非使用原文件名，不符合预期。

---

### 二、优化后的命名规则

点击「选择此目录」导入目录中的视频时：

| 场景 | 视频标题 | 示例 |
|------|---------|------|
| **创建合集**（勾选「自动创建系列」且有 2+ 视频） | `{目录名} 第{N}集` | `电影A 第1集`、`电影A 第2集` |
| **创建合集**（勾选但目录仅 1 个视频） | `{目录名}` | `电影A` |
| **不创建合集**（取消勾选「自动创建系列」） | **原文件名** | `EP01.mp4`、`正片.mp4` |
| **存在 NFO 文件** | **NFO 标题**（优先级最高，覆盖以上规则） | NFO 中定义的标题 |

---

### 三、代码改动（[app.py](file:///D:/文件管理/VIDEO/video_site/app.py#L958-L966)）

`api_batch_import` 函数中，将标题判断条件由 `if series_name:` 改为 `if series_id:`：

```python
# 优化前（旧逻辑）：
if series_name:                    # ← series_name 始终非空，导致不创建系列时也用目录名
    if len(videos) > 1:
        base_title = f"{series_name} 第{idx}集"
    else:
        base_title = series_name
else:
    base_title = name

# 优化后（新逻辑）：
if series_id:                      # ← 仅在真正创建合集时才用目录名
    if len(videos) > 1:
        base_title = f"{series_name} 第{idx}集"
    else:
        base_title = series_name
else:
    base_title = name              # ← 不创建合集时使用原文件名
```

**原因**：前端点击「选择此目录」时始终把目录名作为 `series_name` 传入，但 `series_id` 仅在 `auto_create_series=True` 且实际创建系列后才会被赋值。用 `series_id` 作为判断条件能准确区分"是否创建了合集"。

---

### 四、不受影响的部分

- **NFO 标题优先级不变**：若视频所在目录存在 NFO 文件，NFO 中的标题始终覆盖上述命名（`video_data['title'] = metadata['title']`）
- **单个视频导入（`single_import`）**：始终使用原文件名（或 NFO 标题），不受本次改动影响
- **多选视频导入（`multi_import`）**：始终使用原文件名（或 NFO 标题），不受本次改动影响
- **封面优先级**：NFO 封面 > 用户指定封面，不变
- **合集创建逻辑**：仍由「自动识别并创建系列」复选框控制，不变

---

# 更新说明 - 2026-08-12

## 新增功能：短视频播放标记与去重随机播放 + 仪表盘短视频信息 + 短视频播放画面修复 + 用户密码修改

**更新日期：2026-08-12**

本次更新包含 4 项改动：①短视频播放标记与去重随机播放（Web + APP 互通）；②后台仪表盘新增短视频相关信息；③修复 PC 端竖向短视频播放时画面被裁剪的问题；④用户管理新增修改密码功能。

---

## 一、短视频播放标记与去重随机播放（需求1）

### 1.1 功能机制

- 用户播放过的短视频会被标记为「已播放」（`videos.is_played` 字段）
- 开启随机播放时，已播放的视频被排除在随机列表外
- 当所有短视频都被播放完后，后端**自动清空**播放标记，重新开始新一轮
- Web 端和 APP 端共享同一套标记状态，**多端互通**

### 1.2 数据库变更

`videos` 表新增 `is_played` 字段（INTEGER, DEFAULT 0），应用启动时通过 `ALTER TABLE` 自动添加。

### 1.3 后端函数（[models.py](file:///d:/文件管理/VIDEO/video_site/models.py#L460)）

| 函数 | 说明 |
|------|------|
| `mark_short_played(video_id)` | 标记短视频已播放，全部播完时自动重置 |
| `get_short_played_status(category_id)` | 返回 played_ids / unplayed_ids / 计数 |
| `reset_all_short_played()` | 手动清空所有播放标记 |
| `get_shorts_for_random(category_id, exclude_played, limit)` | 获取未播放短视频列表，空时自动回退全部 |

### 1.4 API 变动（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py)）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/videos/<id>/view` | POST | 增强：短视频播放时同时标记 is_played，返回 `played.reset` 信号 |
| `/api/v1/shorts/<id>/play` | POST | **新增**：APP 专用标记播放接口 |
| `/api/v1/shorts/played` | GET | **新增**：查询播放状态（played_ids 列表） |
| `/api/v1/shorts` | GET | 增强：新增 `exclude_played=1` 参数，返回 `used_fallback` / `played_count` / `total_count` |
| `/api/admin/shorts/reset_played` | POST | **新增**：后台手动清空播放标记 |

### 1.5 前端改动（[shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html)）

- 顶部进度区新增「已播 X / Y」徽章
- 注入后端 `PLAYED_IDS` Set
- `shuffleFeed()` 开启随机播放时先过滤已播放再洗牌，全部播完自动回退
- `playAt()` 播放时本地标记 + 调用后端 API，收到 `reset=true` 时自动刷新页面

### 1.6 仪表盘改动（[dashboard.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/dashboard.html)）

- 原视频总数卡片改为「普通视频数」（不含短视频）
- 新增 4 张短视频卡片：短视频总数 / 短视频分类数 / 短视频总播放 / 已播放短视频
- 新增播放进度条（带百分比 + 全部播完提示）
- 新增「清空播放标记」按钮
- 快捷操作区新增：添加短视频 / 短视频分类 / 进入短视频

---

## 二、PC 端竖向短视频播放画面修复

### 2.1 问题

PC 端播放竖向短视频时画面被拉伸/裁剪，无法显示全部画面。

### 2.2 修复方案（3 个层面）

#### CSS 修复（[style.css](file:///d:/文件管理/VIDEO/video_site/static/css/style.css#L1220-L1226)）

| 改动 | 问题 | 修复 |
|------|------|------|
| `width: 100%; height: 100%` | 强制视频撑满容器，contain 失效 | 改为仅 `max-width/max-height: 100%` |
| `transition: object-fit 0.15s` | 过渡动画导致初始渲染裁剪 | 移除 |
| 全屏 `height: 100vh` | 100vh 在全屏元素内异常 | 改为 `height: 100%` |

#### JS 核心修复（[shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html#L231-L267)）

重写 `applyVideoFit` 函数，不再仅依赖 CSS `object-fit`，而是**精确计算像素尺寸**：

- 竖屏视频（ratio < 1）：以容器高度为基准计算宽度，超出时回退
- 横屏视频（ratio ≥ 1）：以容器宽度为基准计算高度，超出时回退
- 设置 `player.style.width/height` 为精确像素值

#### 触发时机补齐（4 个场景全覆盖）

| 时机 | 说明 |
|------|------|
| `loadedmetadata` 事件 | 视频元数据加载后适配 |
| `playAt(index)` 切换视频 | **新增**：每次切换活动视频时重新适配 |
| `window.resize` 事件 | 窗口尺寸变化时重新适配 |
| `fullscreenchange` 事件 | **新增**：全屏切换后延迟 300ms 重新适配 |

---

## 三、用户管理新增修改密码功能

### 3.1 数据层（[models.py](file:///d:/文件管理/VIDEO/video_site/models.py#L157-L162)）

新增 `update_password(user_id, new_password)` 函数，通过 `hash_password` 加密后更新数据库。

### 3.2 API 路由（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py#L1275-L1285)）

新增 `PUT /api/users/<user_id>/password` 接口：
- 需管理员权限
- 校验密码非空且不少于 6 位
- 当前用户也可修改自己的密码

### 3.3 前端页面（[users.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/users.html#L78-L92)）

- 每行用户操作列新增 🔑 修改密码按钮（包括当前用户）
- 点击弹出 Bootstrap 模态框，需输入新密码 + 确认密码
- 前端校验：非空、≥6位、两次输入一致
- 支持回车键提交

---

## 四、受影响文件清单

| 文件 | 改动内容 |
|------|---------|
| [models.py](file:///d:/文件管理/VIDEO/video_site/models.py) | 新增 `is_played` 字段、4 个播放标记函数、`update_password` 函数、`get_video_stats` 增强 |
| [app.py](file:///d:/文件管理/VIDEO/video_site/app.py) | 新增 3 个 API 接口 + 增强 2 个接口 + import 增加 `update_password` |
| [templates/shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html) | 播放标记 UI + 随机去重逻辑 + `applyVideoFit` 重写 + 全屏/切换重新适配 |
| [templates/admin/dashboard.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/dashboard.html) | 短视频概览卡片 + 进度条 + 重置按钮 + 快捷入口 |
| [templates/admin/users.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/users.html) | 修改密码按钮 + 模态框 + JS 逻辑 |
| [static/css/style.css](file:///d:/文件管理/VIDEO/video_site/static/css/style.css) | `.short-video` 样式修复 + 新增配色类 |
| [APP_UPDATE_20260812.md](file:///d:/文件管理/VIDEO/video_site/APP_UPDATE_20260812.md) | APP 端功能更新提示词（新增文件） |
| [UPDATE_20260808.md](file:///d:/文件管理/VIDEO/video_site/UPDATE_20260808.md) | 本文件，追加 2026-08-12 更新说明 |

---

## 五、APP 端对接

已生成完整的 APP 端功能更新提示词文档：[APP_UPDATE_20260812.md](file:///d:/文件管理/VIDEO/video_site/APP_UPDATE_20260812.md)

包含 3 个对接接口、5 个功能点、8 条验收标准、Android Kotlin 参考代码。

---

# 更新说明 - 2026-08-13

## 新增功能：用户分类权限分配 + 短视频收藏 + APP 配套 API

**更新日期：2026-08-13**

本次更新包含 3 项功能：①用户分类权限分配（区分长视频/短视频可查看分类）；②短视频收藏功能（播放页收藏按钮 + 后台收藏管理）；③配套 APP 使用的 API 接口。

---

## 一、用户分类权限分配（需求1）

### 1.1 功能机制

- 管理员可为每个用户分别设置可查看的「长视频分类」和「短视频分类」
- 不勾选任何分类 = 该类型全部可见（默认）
- 勾选分类后，该用户只能查看被勾选的分类中的视频
- 权限通过用户名/密码识别，APP 端登录后即可获取对应权限

### 1.2 数据库变更

新增 `user_category_permissions` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER | 用户 ID |
| category_id | INTEGER | 分类 ID |
| category_type | TEXT | 'normal'（长视频分类）或 'short'（短视频分类） |
| created_at | TIMESTAMP | 创建时间 |

UNIQUE(user_id, category_id, category_type) 防止重复。

### 1.3 后端函数（[models.py](file:///d:/文件管理/VIDEO/video_site/models.py#L743)）

| 函数 | 说明 |
|------|------|
| `set_user_permissions(user_id, category_ids, category_type)` | 覆盖式设置用户某类型的分类权限 |
| `get_user_permissions(user_id)` | 获取用户 normal/short 两种类型的分类权限 |
| `get_user_allowed_category_ids(user_id, category_type)` | 获取用户某类型可查看的分类ID列表（None=全部可见） |

### 1.4 API 路由（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py#L1309)）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/users/<id>/permissions` | GET | 获取用户分类权限 |
| `/api/users/<id>/permissions` | PUT | 设置用户分类权限（normal_category_ids + short_category_ids） |

### 1.5 前端改动（[users.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/users.html)）

- 用户列表操作列新增 🔒 分类权限按钮
- 点击弹出 Bootstrap 模态框，分两个区域显示长视频分类和短视频分类的复选框
- 打开时自动加载当前权限并勾选
- 底部说明「不勾选任何分类 = 该类型全部可见（默认）」

### 1.6 Session 改动

login 路由新增 `session['user_id'] = user['id']`，使收藏和权限功能能识别当前用户。

---

## 二、短视频收藏功能（需求2）

### 2.1 功能机制

- 短视频播放页右上角新增 ⭐ 五角星收藏按钮
- 点击切换收藏/取消收藏，已收藏时图标变金色
- 后台新增「收藏管理」菜单（系列管理下方），功能同分类管理
- 收藏是用户维度的，不同用户有各自独立的收藏列表

### 2.2 数据库变更

新增 `favorites` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER | 用户 ID |
| video_id | INTEGER | 视频 ID |
| created_at | TIMESTAMP | 收藏时间 |

UNIQUE(user_id, video_id) 防止重复收藏。

### 2.3 后端函数（[models.py](file:///d:/文件管理/VIDEO/video_site/models.py#L791)）

| 函数 | 说明 |
|------|------|
| `add_favorite(user_id, video_id)` | 添加收藏 |
| `remove_favorite(user_id, video_id)` | 取消收藏 |
| `is_favorited(user_id, video_id)` | 检查是否已收藏 |
| `get_user_favorite_ids(user_id)` | 获取用户已收藏的视频ID集合 |
| `get_all_favorites(page, per_page, keyword)` | 后台管理：分页查询所有收藏 |
| `delete_favorite(favorite_id)` | 删除单条收藏 |
| `batch_delete_favorites(ids)` | 批量删除收藏 |

### 2.4 API 路由

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/favorites/<video_id>` | POST | 切换收藏状态 |
| `/api/favorites` | GET | 获取当前用户收藏的视频ID列表 |
| `/api/favorites/<favorite_id>` | DELETE | 后台删除单条收藏 |
| `/api/favorites/batch_delete` | POST | 后台批量删除收藏 |

### 2.5 前端改动

#### 播放页（[shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html)）

- 每个 `.short-video-container` 内新增 ⭐ 收藏按钮（`top:108px; right:12px`）
- 页面加载时 fetch `/api/favorites` 获取已收藏列表，更新按钮状态
- 点击按钮调用 `POST /api/favorites/<video_id>` 切换收藏
- 已收藏：图标 `bi-star-fill`，金色；未收藏：图标 `bi-star`，白色

#### CSS（[style.css](file:///d:/文件管理/VIDEO/video_site/static/css/style.css#L1295)）

新增 `.short-favorite-btn` 样式：圆形半透明背景，悬停放大，已收藏金色。

#### 后台收藏管理页（[favorites.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/favorites.html)）新增

- 搜索框（按视频标题/用户名搜索）
- 表格：复选框 | ID | 用户名 | 视频标题（含封面缩略图）| 类型 | 收藏时间 | 删除
- 类型列：短视频橙色 badge / 普通视频蓝色 badge
- 分页 + 批量删除

#### 侧边栏更新

所有 9 个后台模板的侧边栏「系列管理」下方新增「收藏管理」菜单项（`bi-star` 图标）。

---

## 三、APP 配套 API（需求3）

### 3.1 新增 V1 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/login` | POST | APP 登录，返回用户信息 + 分类权限 |
| `/api/v1/user/categories` | GET | 获取当前用户可查看的分类权限 |
| `/api/v1/shorts/<id>/favorite` | POST | APP 收藏/取消收藏短视频 |
| `/api/v1/favorites` | GET | 获取当前用户收藏的视频ID列表 |

### 3.2 鉴权方式

- `POST /api/v1/login`：请求体 `{username, password}`，返回 `permissions`
- `GET /api/v1/user/categories`、`POST /api/v1/shorts/<id>/favorite`、`GET /api/v1/favorites`：支持两种鉴权
  - 方式1：Web session（已登录用户）
  - 方式2：HTTP Basic Auth header（`Authorization: Basic base64(username:password)`）

### 3.3 响应示例

**登录响应**：
```json
{
  "success": true,
  "user": {"id": 1, "username": "admin", "role": "admin"},
  "permissions": {"normal": [1, 2], "short": [3, 5]}
}
```
permissions 中空数组表示该类型全部可见。

**收藏切换响应**：
```json
{"success": true, "favorited": true}
```

---

## 四、受影响文件清单

| 文件 | 改动内容 |
|------|---------|
| [models.py](file:///d:/文件管理/VIDEO/video_site/models.py) | 新增 2 张表 + 10 个函数（权限 3 个 + 收藏 7 个） |
| [app.py](file:///d:/文件管理/VIDEO/video_site/app.py) | import 增加 10 个函数 + session 存储 user_id + 新增 8 个 API 路由 + 1 个后台路由 |
| [templates/admin/users.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/users.html) | 新增分类权限按钮 + 模态框 + JS 逻辑 |
| [templates/admin/favorites.html](file:///d:/文件管理/VIDEO/video_site/templates/admin/favorites.html) | 新增收藏管理页面（新增文件） |
| [templates/shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html) | 新增收藏按钮 + 收藏状态同步 + toggleFavorite 函数 |
| [static/css/style.css](file:///d:/文件管理/VIDEO/video_site/static/css/style.css) | 新增 `.short-favorite-btn` 样式 |
| 9 个后台模板 | 侧边栏新增「收藏管理」菜单项 |
| [UPDATE_20260808.md](file:///d:/文件管理/VIDEO/video_site/UPDATE_20260808.md) | 追加 2026-08-13 更新说明 |

