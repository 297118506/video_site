# 更新说明 - 2026-08-08

## 新增功能：短视频分类 + 短·分类管理

本次更新围绕**短视频分类独立化**和**短视频模块体验优化**展开，核心改动是：为短视频建立独立的分类体系（short_categories），与原有的视频分类（categories）完全隔离，互不干扰。

---

## 一、短视频模块改造

### 1.1 路由变化

### 1.2 短视频分类列表页

- 页面顶部大标题「短视频分类」
- 网格卡片布局，自适应列数
- 第一张卡片固定为「全部」橙红渐变配色，显示总视频数量
- 其它分类按"父分类→子分类"顺序展示
- 父分类会统计其子分类的视频总数，并显示「含子分类」标签
- 卡片悬浮上浮效果，视觉体验更好
- 空状态时引导用户「去后台创建分类」或「直接播放全部」

### 1.3 播放页增强

- 顶部栏**左侧**新增：
  - 返回按钮（←）：返回分类列表
  - 当前分类标签（图标 + 名称）
  - 分类下拉选择器：不离开播放页即可切换到其它分类
- 顶部栏**右侧**保持不变：自动切换开关、随机播放开关、进度指示 (1/N)
- 顶部栏布局由 `justify-content: center` 改为 `space-between`

---

## 二、后台管理：短·分类管理

### 2.1 菜单位置

在**所有后台页面**的侧边栏，「分类管理」下方新增「短·分类管理」：

```
仪表盘
视频管理
短视频管理
分类管理
短·分类管理 ← 新增
系列管理
OpenList管理
用户管理
```

### 2.2 功能特性

与「分类管理」完全一致，但操作的是独立的数据库表：

- 添加分类（支持父子层级、排序）
- 列表层级展示（父分类加粗，子分类缩进显示「├─」）
- 单个删除 + 批量删除（带确认提示）
- 分页（每页 20 条）
- 删除分类时会同时删除该分类下的短视频（仅 短视频的记录，不影响普通视频）

### 2.3 标题说明

短·分类管理页面标题带副标题提示：
> 短·分类管理（仅用于短视频模块，与普通分类互不干扰）

### 2.4 短视频管理页面联动

- 「短视频管理」列表页的分类筛选，下拉选项变为**短分类**
- 「添加短视频」表单的分类选择框，变为**短分类**
- 「编辑短视频」表单同上

---

## 三、OpenList 导入：分类选择联动

### 3.1 自动切换分类源

根据「导入到模块」下拉的选择，自动切换分类下拉的选项


### 3.2 原有功能保持

- 切换到短视频模块仍然会：
  - 禁用/取消「自动创建系列」复选框
  - 隐藏文件列表中的图片
  - 禁用封面选择

---

## 四、APP 开放 API 新增接口

### 4.1 `/api/v1/short_categories` (新增)

获取短视频分类树，返回分类、每个分类的视频数量统计，以及短视频总数。


### 4.2 `/api/v1/shorts` 增强


### 4.3 全部 V1 API 汇总

| 接口 | 说明 |
|------|------|
| `GET /api/v1/categories` | 普通视频分类树 |
| `GET /api/v1/short_categories` | 短视频分类树 + 数量统计 (新增) |
| `GET /api/v1/videos` | 普通视频列表 |
| `GET /api/v1/videos/<id>` | 视频详情 |
| `GET /api/v1/shorts?category_id=...` | 短视频列表，支持短分类筛选 (增强) |
| `GET /api/v1/series` | 系列列表 |
| `GET /api/v1/series/<id>/episodes` | 系列分集 |
| `GET /api/v1/stats` | 统计数据 |
| `GET /api/v1/search?q=&type=all|video|shorts` | 全局搜索 |

---

## 五、数据库变更

### 5.1 新增表 `short_categories`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| name | TEXT NOT NULL | 分类名称 |
| slug | TEXT UNIQUE | URL 友好标识 |
| parent_id | INTEGER DEFAULT 0 | 父分类 ID (0=顶级) |
| sort_order | INTEGER DEFAULT 0 | 排序权重 |
| created_at | TIMESTAMP | 创建时间 |

与 `categories` 表结构完全相同，但独立存在。应用启动时通过 `CREATE TABLE IF NOT EXISTS` 自动创建，无需手工迁移。

### 5.2 videos 表结构不变

- `category_id` 仍然使用同一列，但语义根据 `is_short_video` 的值自动区分：
  - `is_short_video=1` 的视频 → `category_id` 指向 `short_categories.id`
  - `is_short_video=0` 的视频 → `category_id` 指向 `categories.id`

---


# 更新说明 - 2026-08-10

## 新增功能：OpenList 多选视频导入

**更新日期：2026-08-10**

本次更新优化了 OpenList 管理中的视频导入功能，支持在目录浏览时同时多选多个视频文件一次性导入，无需逐个导入或整目录导入。

---

### 一、功能说明

此前在 OpenList 文件列表中只能：
- 点击「选择此目录」批量导入整个目录下所有视频（会创建系列）
- 点击单个视频导入单个文件

本次新增**多选导入**模式：在目录下有多个视频时，用户可点击视频项自由选择一个或多个视频，再一次性导入。导入的视频使用原文件名作为标题（与单个视频导入逻辑一致），不创建系列。

---

### 二、前端改动

#### 2.1 状态变量调整

将原单个视频选择变量 `selectedVideoFile`（对象）改为数组 `selectedVideoFiles`，支持存储多个选中项


#### 2.2 视频文件项 UI

每个视频文件项添加复选框，支持两种选择方式：
- **点击整行**：切换选中/取消选中状态
- **点击复选框**：同样切换状态（`event.stopPropagation()` 避免重复触发）

选中状态视觉反馈：
- 整行背景色变为浅绿色
- 复选框勾选状态同步更新

---

### 三、后端改动

#### 3.1 新增接口 `POST /api/openlist/multi_import`

#### 3.2 与现有接口的对比

| 接口 | 用途 | 是否创建系列 | 标题来源 |
|------|------|------------|---------|
| `POST /api/openlist/single_import` | 导入单个视频 | 否 | 文件名 / NFO 标题 |
| `POST /api/openlist/multi_import` (新增) | 导入多个选中视频 | 否 | 文件名 / NFO 标题 |
| `POST /api/openlist/batch_import` | 导入整个目录 | 可选（按集数） | 目录名+集数 / NFO 标题 |

---

### 四、保持不变的约束

遵循项目既定规则：
- 多选导入**不创建系列**（仅 `batch_import` 在勾选「自动创建系列」时才会创建）
- 标题优先级：NFO 标题 > 原文件名
- 封面优先级：NFO 封面 > 用户指定封面
- 短视频模块下导入时，文件列表仍隐藏图片、禁用封面选择、禁用自动创建系列
- 已存在的视频（按 `video_url` 去重）自动跳过

---

# 更新说明 - 2026-08-10 (二)

## 优化：「选择此目录」导入时的视频命名逻辑

**更新日期：2026-08-10**

---

### 一、优化背景

此前在 OpenList 管理中点击「选择此目录」批量导入视频时，**无论是否创建合集**，视频标题一律使用「目录名 第N集」格式。这导致用户取消「自动创建系列」后导入的视频仍被命名为"目录名 第1集"等，而非使用原文件名，不符合预期。

---

### 二、优化后的命名规则

点击「选择此目录」导入目录中的视频时：

| 场景 | 视频标题 | 示例 |
|------|---------|------|
| **创建合集**（勾选「自动创建系列」且有 2+ 视频） | `{目录名} 第{N}集` | `电影A 第1集`、`电影A 第2集` |
| **创建合集**（勾选但目录仅 1 个视频） | `{目录名}` | `电影A` |
| **不创建合集**（取消勾选「自动创建系列」） | **原文件名** | `EP01.mp4`、`正片.mp4` |
| **存在 NFO 文件** | **NFO 标题**（优先级最高，覆盖以上规则） | NFO 中定义的标题 |

---

# 更新说明 - 2026-08-12

## 新增功能：短视频播放标记与去重随机播放 + 仪表盘短视频信息 + 短视频播放画面修复 + 用户密码修改

**更新日期：2026-08-12**

本次更新包含 4 项改动：①短视频播放标记与去重随机播放（Web + APP 互通）；②后台仪表盘新增短视频相关信息；③修复 PC 端竖向短视频播放时画面被裁剪的问题；④用户管理新增修改密码功能。

---

## 一、短视频播放标记与去重随机播放

### 1.1 功能机制

- 用户播放过的短视频会被标记为「已播放」
- 开启随机播放时，已播放的视频被排除在随机列表外
- 当所有短视频都被播放完后，后端**自动清空**播放标记，重新开始新一轮
- Web 端和 APP 端共享同一套标记状态，**多端互通**

### 1.2 数据库变更

`videos` 表新增 `is_played` 字段（INTEGER, DEFAULT 0），应用启动时通过 `ALTER TABLE` 自动添加。

### 1.3 API 变动（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py)）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/videos/<id>/view` | POST | 增强：短视频播放时同时标记 is_played，返回 `played.reset` 信号 |
| `/api/v1/shorts/<id>/play` | POST | **新增**：APP 专用标记播放接口 |
| `/api/v1/shorts/played` | GET | **新增**：查询播放状态（played_ids 列表） |
| `/api/v1/shorts` | GET | 增强：新增 `exclude_played=1` 参数，返回 `used_fallback` / `played_count` / `total_count` |
| `/api/admin/shorts/reset_played` | POST | **新增**：后台手动清空播放标记 |

### 1.5 前端改动

- 顶部进度区新增「已播 X / Y」徽章

### 1.6 仪表盘改动

- 原视频总数卡片改为「普通视频数」（不含短视频）
- 新增 4 张短视频卡片：短视频总数 / 短视频分类数 / 短视频总播放 / 已播放短视频
- 新增播放进度条（带百分比 + 全部播完提示）
- 新增「清空播放标记」按钮
- 快捷操作区新增：添加短视频 / 短视频分类 / 进入短视频

---

## 二、PC 端竖向短视频播放画面修复

### 2.1 问题

PC 端播放竖向短视频时画面被拉伸/裁剪，无法显示全部画面。

### 2.2 修复方案

#### CSS 修复

| 改动 | 问题 | 修复 |
|------|------|------|
| `width: 100%; height: 100%` | 强制视频撑满容器，contain 失效 | 改为仅 `max-width/max-height: 100%` |
| `transition: object-fit 0.15s` | 过渡动画导致初始渲染裁剪 | 移除 |
| 全屏 `height: 100vh` | 100vh 在全屏元素内异常 | 改为 `height: 100%` |

#### JS 核心修复

重写 `applyVideoFit` 函数，不再仅依赖 CSS `object-fit`，而是**精确计算像素尺寸**：

- 竖屏视频（ratio < 1）：以容器高度为基准计算宽度，超出时回退
- 横屏视频（ratio ≥ 1）：以容器宽度为基准计算高度，超出时回退
- 设置 `player.style.width/height` 为精确像素值

#### 触发时机补齐

| 时机 | 说明 |
|------|------|
| `loadedmetadata` 事件 | 视频元数据加载后适配 |
| `playAt(index)` 切换视频 | **新增**：每次切换活动视频时重新适配 |
| `window.resize` 事件 | 窗口尺寸变化时重新适配 |
| `fullscreenchange` 事件 | **新增**：全屏切换后延迟 300ms 重新适配 |

---

## 三、用户管理新增修改密码功能

### 3.1 数据层

新增 `update_password(user_id, new_password)` 函数，通过 `hash_password` 加密后更新数据库。

### 3.2 API 路由

新增 `PUT /api/users/<user_id>/password` 接口：
- 需管理员权限
- 校验密码非空且不少于 6 位
- 当前用户也可修改自己的密码

### 3.3 前端页面

- 每行用户操作列新增 🔑 修改密码按钮（包括当前用户）
- 点击弹出 Bootstrap 模态框，需输入新密码 + 确认密码
- 前端校验：非空、≥6位、两次输入一致
- 支持回车键提交

---

# 更新说明 - 2026-08-13

## 新增功能：用户分类权限分配 + 短视频收藏 + APP 配套 API

**更新日期：2026-08-13**

本次更新包含 3 项功能：①用户分类权限分配（区分长视频/短视频可查看分类）；②短视频收藏功能（播放页收藏按钮 + 后台收藏管理）；③配套 APP 使用的 API 接口。

---

## 一、用户分类权限分配

### 1.1 功能机制

- 管理员可为每个用户分别设置可查看的「长视频分类」和「短视频分类」
- 不勾选任何分类 = 该类型全部可见（默认）
- 勾选分类后，该用户只能查看被勾选的分类中的视频
- 权限通过用户名/密码识别，APP 端登录后即可获取对应权限

### 1.2 数据库变更

新增 `user_category_permissions` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER | 用户 ID |
| category_id | INTEGER | 分类 ID |
| category_type | TEXT | 'normal'（长视频分类）或 'short'（短视频分类） |
| created_at | TIMESTAMP | 创建时间 |

UNIQUE(user_id, category_id, category_type) 防止重复。

### 1.3 后端函数
| 函数 | 说明 |
|------|------|
| `set_user_permissions(user_id, category_ids, category_type)` | 覆盖式设置用户某类型的分类权限 |
| `get_user_permissions(user_id)` | 获取用户 normal/short 两种类型的分类权限 |
| `get_user_allowed_category_ids(user_id, category_type)` | 获取用户某类型可查看的分类ID列表（None=全部可见） |

### 1.4 API 路由

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/users/<id>/permissions` | GET | 获取用户分类权限 |
| `/api/users/<id>/permissions` | PUT | 设置用户分类权限（normal_category_ids + short_category_ids） |

### 1.5 前端改动

- 用户列表操作列新增 🔒 分类权限按钮
- 点击弹出 Bootstrap 模态框，分两个区域显示长视频分类和短视频分类的复选框
- 打开时自动加载当前权限并勾选
- 底部说明「不勾选任何分类 = 该类型全部可见（默认）」

### 1.6 Session 改动

login 路由新增 `session['user_id'] = user['id']`，使收藏和权限功能能识别当前用户。

---

## 二、短视频收藏功能

### 2.1 功能机制

- 短视频播放页右上角新增 ⭐ 五角星收藏按钮
- 点击切换收藏/取消收藏，已收藏时图标变金色
- 后台新增「收藏管理」菜单（系列管理下方），功能同分类管理
- 收藏是用户维度的，不同用户有各自独立的收藏列表

### 2.2 数据库变更

新增 `favorites` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER | 用户 ID |
| video_id | INTEGER | 视频 ID |
| created_at | TIMESTAMP | 收藏时间 |

UNIQUE(user_id, video_id) 防止重复收藏。

### 2.3 后端函数

| 函数 | 说明 |
|------|------|
| `add_favorite(user_id, video_id)` | 添加收藏 |
| `remove_favorite(user_id, video_id)` | 取消收藏 |
| `is_favorited(user_id, video_id)` | 检查是否已收藏 |
| `get_user_favorite_ids(user_id)` | 获取用户已收藏的视频ID集合 |
| `get_all_favorites(page, per_page, keyword)` | 后台管理：分页查询所有收藏 |
| `delete_favorite(favorite_id)` | 删除单条收藏 |
| `batch_delete_favorites(ids)` | 批量删除收藏 |

### 2.4 API 路由

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/favorites/<video_id>` | POST | 切换收藏状态 |
| `/api/favorites` | GET | 获取当前用户收藏的视频ID列表 |
| `/api/favorites/<favorite_id>` | DELETE | 后台删除单条收藏 |
| `/api/favorites/batch_delete` | POST | 后台批量删除收藏 |

### 2.5 前端改动

#### 播放页

- 每个 `.short-video-container` 内新增 ⭐ 收藏按钮（`top:108px; right:12px`）
- 页面加载时 fetch `/api/favorites` 获取已收藏列表，更新按钮状态
- 点击按钮调用 `POST /api/favorites/<video_id>` 切换收藏
- 已收藏：图标 `bi-star-fill`，金色；未收藏：图标 `bi-star`，白色

#### CSS

新增 `.short-favorite-btn` 样式：圆形半透明背景，悬停放大，已收藏金色。

#### 后台收藏管理页

- 搜索框（按视频标题/用户名搜索）
- 表格：复选框 | ID | 用户名 | 视频标题（含封面缩略图）| 类型 | 收藏时间 | 删除
- 类型列：短视频橙色 badge / 普通视频蓝色 badge
- 分页 + 批量删除

#### 侧边栏更新

所有 9 个后台模板的侧边栏「系列管理」下方新增「收藏管理」菜单项。

---

## 三、APP 配套 API

### 3.1 新增 V1 API 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/login` | POST | APP 登录，返回用户信息 + 分类权限 |
| `/api/v1/user/categories` | GET | 获取当前用户可查看的分类权限 |
| `/api/v1/shorts/<id>/favorite` | POST | APP 收藏/取消收藏短视频 |
| `/api/v1/favorites` | GET | 获取当前用户收藏的视频ID列表 |

---

# 更新说明 - 2026-08-17 至 2026-08-20

## 优化与修复合集

**更新日期：2026-08-20**

## 一、短视频播放器黑屏修复

### 1.1 问题描述

部分短视频播放时黑屏（有声音、进度条前进、无画面）。

### 1.2 根因

`.short-video` CSS 去掉了 `width:100%; height:100%`，加上 JS `applyVideoFit()` 在容器尺寸为 0（屏幕外视频）时直接 return 不设置任何尺寸，导致视频元素 0×0 → 黑屏。

### 1.3 修复

**[style.css](file:///d:/文件管理/VIDEO/video_site/static/css/style.css#L1220-L1226)**：恢复 `.short-video { width:100%; height:100%; object-fit:contain; }` 兜底，浏览器原生保证不裁剪 + 完整显示。

**[shorts.html](file:///d:/文件管理/VIDEO/video_site/templates/shorts.html#L277-L291)**：`applyVideoFit()` 简化为仅设置 `object-fit: contain` 并清除残留固定像素尺寸，不再手动计算像素。

---

## 二、EXE 打包支持

### 2.1 改动文件

| 文件 | 改动 |
|------|------|
| [config.py](file:///d:/文件管理/VIDEO/video_site/config.py) | 新增 `_get_base_dir()` / `_get_resource_dir()`，EXE 模式下数据库路径指向 EXE 同级目录，模板/静态文件从 `_MEIPASS` 加载 |
| [app.py](file:///d:/文件管理/VIDEO/video_site/app.py#L27-L29) | Flask 初始化显式指定 `template_folder` / `static_folder` 从打包资源加载 |
| [run.py](file:///d:/文件管理/VIDEO/video_site/run.py) | EXE 模式下关闭 debug、自动创建 data 目录、自动打开浏览器 |
| [build_exe.spec](file:///d:/文件管理/VIDEO/video_site/build_exe.spec) | PyInstaller 打包配置（打包 templates + static 到资源目录） |

### 2.2 使用方式

- 双击 `dist/视频管理系统.exe` 即可运行，无需 Python 环境
- 数据库自动在 EXE 同级 `data/` 目录创建
- 默认账号：admin / password

---

## 三、OpenList 外网 HTTPS 连接修复 + 文件预览

### 3.1 HTTPS 连接修复

**问题**：外网 HTTPS OpenList 返回结构与内网不同，`resp.json()['data']['token']` 中 `data` 为 `None` 时直接报错 `'NoneType' object is not subscriptable`。

**修复**：
- 新增 `_post()` 统一封装，所有请求使用 `verify=False` 兼容自签名 HTTPS 证书
- `login()` 改为安全解析：`body.get('data')` 检查非 None 且为 dict，再取 `token`
- `get_file_list()` 和 `get_direct_link()` 同样改为安全取值
- 全局禁用 SSL 警告

### 3.2 文件预览功能

**新增预览按钮**：视频和图片文件行，在「类型标签」前增加 👁 眼睛按钮。

**支持的预览格式**：

| 类型 | 支持的格式 |
|------|-----------|
| 视频 | mp4、webm、mov、m4v、avi、wmv、flv、ts、rmvb |
| HLS | m3u8（使用 hls.js，Safari 使用原生播放） |
| 图片 | jpg、jpeg、png、gif、bmp、webp、svg、ico、tiff、tif、heic、avif |
| 其他 | 显示下载按钮 |

**预览弹窗特性**：
- Bootstrap Modal 弹窗，黑色背景适配观看
- 视频自动播放 + controls 控件
- 图片最大 75vh 高度，`object-fit:contain` 保持比例
- HLS 使用 hls.js 播放（Chrome/Edge），Safari 使用原生播放
- 加载失败显示友好提示
- 点击背景 / ESC 键 / 关闭按钮 均可关闭
- 关闭时自动销毁 HLS 实例、暂停视频、清理 DOM

**新增 JS 函数**：
- `previewFile(filePath, fileName)` — 获取直链 + 判断类型 + 打开弹窗
- `showPreviewModal(fileUrl, fileName, isVideo, isHls, isImage)` — 构建 Modal + 加载资源
- `closePreviewModal()` — 清理 HLS 实例、暂停视频、移除 DOM

---

# 更新说明 - 2026-08-20（二）

## 新增功能：短视频播放标记按用户维度独立记录

**更新日期：2026-08-20**

本次更新修复了**不同用户共享同一份短视频播放标记**的问题：原设计中所有用户共用 `videos.is_played` 全局字段，导致用户 A 随机播放过的 50 个短视频，在用户 B 切换登录后也被识别为「已播放」，使得用户 B 的随机播放列表被错误地排除掉用户 A 看过的视频。

修复后，每位用户的播放标记独立存储在 `user_short_played` 表中，互不影响；同时仪表盘展示和清空操作同步升级为「按用户维度」。

---

## 一、数据库变更

### 1.1 新增表 `user_short_played`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| user_id | INTEGER NOT NULL | 用户 ID（关联 users.id） |
| video_id | INTEGER NOT NULL | 短视频 ID（关联 videos.id） |
| created_at | TIMESTAMP | 标记时间 |

`UNIQUE(user_id, video_id)` 唯一约束，避免重复插入。

### 1.2 旧字段保留

`videos.is_played` 字段保留作为**匿名用户**的回退标记（未登录场景），同时仍可被「清空全部」操作重置。

应用启动时通过 `CREATE TABLE IF NOT EXISTS` 自动建表，无需手工迁移。

---

## 二、数据层改动

### 2.1 4 个核心函数全部支持 `user_id` 参数

| 函数 | 旧签名 | 新签名 | 行为变化 |
|------|--------|--------|----------|
| `mark_short_played` | `(video_id)` | `(video_id, user_id=None)` | 传入 user_id 写入 `user_short_played` 表；None 则回退全局 `is_played` 字段 |
| `get_short_played_status` | `(category_id=None)` | `(category_id=None, user_id=None)` | 按用户维度返回已播放 ID 列表 |
| `reset_all_short_played` | `()` | `(user_id=None)` | 传入则仅清空该用户；None 则清空全部 + 重置全局字段 |
| `get_shorts_for_random` | `(category_id, exclude_played, limit)` | `(..., user_id=None)` | 随机播放按用户维度排除已播放 |

### 2.2 新增统计函数 `get_all_users_short_played_stats()`

为仪表盘提供数据：遍历所有用户，结合其分类权限计算：

- **可访问短视频总数**：基于 `user_category_permissions` 表的 `short` 类型权限过滤后的 `videos` 数量
- **已播放数**：`user_short_played` 中该用户的记录数（同时按权限过滤）
- **进度百分比**：`played / total * 100`，保留 1 位小数

**权限规则**：若用户未设置任何 short 分类权限 → 全部短视频可见；否则仅统计权限内分类的视频。

### 2.3 自动清空逻辑

`mark_short_played` 在标记后发现该用户**可访问的短视频已全部播放完毕**时，自动清空该用户的标记，使下一轮随机播放可以重新从全部视频开始。这是为了让随机播放形成「看完一轮 → 重新开始」的循环体验。

---

## 三、API 路由改动

### 3.1 新增辅助函数 `_get_current_user_id()`

**适配两种场景**：
- Web 后台用户：从 Flask session 读取
- APP 用户：从 HTTP Basic Auth 解析

### 3.2 5 处调用点传入 `user_id`

| 路由 | 函数 | 行号参考 |
|------|------|---------|
| `GET /shorts/play/<category_id>` | 播放页获取播放状态 | 调用 `get_short_played_status(user_id=uid)` |
| `GET /api/v1/shorts/<id>` | 短视频详情 | `mark_short_played(video_id, user_id=uid)` |
| `POST /api/v1/shorts/<id>/play` | APP 标记播放 | 同上 |
| `GET /api/v1/shorts/played` | 查询播放状态 | `get_short_played_status(user_id=uid)` |
| `GET /api/v1/shorts`（带 `exclude_played=1`） | 随机播放列表 | `get_shorts_for_random(user_id=uid)` |

### 3.3 清空接口增强


### 3.4 仪表盘路由传参

`admin_dashboard()` 调用 `get_all_users_short_played_stats()` 并将结果通过 `user_played_stats` 注入模板。

---

## 四、仪表盘 UI 改造

### 4.1 旧版 UI（已替换）

单一进度条 + 单个「清空播放标记」按钮：

```
已播放 75 / 160  [████████░░] 46.9%   [清空播放标记]
```

### 4.2 新版 UI

**卡片标题**：`短视频播放进度（按用户独立记录，随机播放去重用）`

**右上角按钮**：`清空全部用户标记` → 调用 `resetShortPlayedAll()`

**主体表格**（每行一位用户）：

| # | 用户名 | 已播放 / 总数 | 进度 | 操作 |
|---|--------|---------------|------|------|
| 1 | admin | 50 / 160 | ████░░ 31.3% | [清空] |
| 2 | user_a | 75 / 120 | █████░ 62.5% | [清空] |
| 3 | user_b | 0 / 80 | ░░░░░ 0% | [清空] |

**每行「清空」按钮** → 调用 `resetShortPlayedUser(userId, username)`

**底部说明**：
> 每个用户的播放标记独立记录，用户A播放过的短视频不会影响用户B的随机播放。

---

## 五、行为兼容性说明

1. **未登录用户 / 匿名访问**：`user_id = None`，回退到原 `videos.is_played` 全局字段，行为与本次更新前一致
2. **APP 端**：只要在请求中携带 Basic Auth，服务端自动按用户维度记录标记，APP 端无需任何代码改动即可享受独立标记
3. **后台管理员**：在仪表盘可看到每个用户的进度，并支持精准清空单个用户或全部用户
4. **随机播放自动重置**：当某用户可访问的短视频全部播放完毕，系统自动清空该用户的标记，重新开始一轮随机播放

---

# 更新说明 - 2026-08-20（三）

## Bug 修复：APP 短视频「全部」卡片点击后总数未按用户权限过滤

**更新日期：2026-08-20**

### 一、问题描述

**复现场景**：10 个短分类共 5000 个视频。用户 D 仅有「分类 A、B、C」三个分类的权限（合计 1500 个）。

- 分类列表页的「全部」卡片**正确**显示 1500（由 `/api/v1/short_categories` 返回 + APP 本地累加）
- 点击「全部」进入播放页，顶部进度信息显示「已播 10 / **5000**」（错误，应为 1500）

**根因**：后端 `/api/v1/shorts?category_id=0`、`/api/v1/shorts?category_id=0&exclude_played=1`、`/api/v1/shorts/played?category_id=0` 三个 APP 接口在 `category_id=0`（全部）时**没有按用户分类权限过滤**，直接返回全量 5000 个视频的统计。

**附带发现**：Web 端 `/shorts/play/0` 路由已正确过滤了 `videos` 列表与 `total`（参考 [app.py:281-290](file:///d:/文件管理/VIDEO/video_site/app.py#L281-L290)），但同一处的 `played_status = get_short_played_status(None, user_id=uid)` 没传权限，导致 Web 端「已播 X / Y」中的 Y 也错误，本次顺手修复。

### 二、修复方案

在**服务端**按用户权限过滤，与 Web 端 `shorts_play` 路由、仪表盘 `get_all_users_short_played_stats` 的现有逻辑保持一致。

**核心改动**：在 `models.py` 的 3 个查询函数新增 `allowed_category_ids` 参数；在 `app.py` 的 4 个调用点获取权限后传入。

### 三、数据层改动（[models.py](file:///d:/文件管理/VIDEO/video_site/models.py)）

| 函数 | 改动 |
|------|------|
| `get_all_videos` | 新增 `category_ids=None` 参数（复数列表），与单个 `category_id` 互斥；为非空列表时拼接 `category_id IN (?,?,?)` |
| `get_short_played_status` | 新增 `allowed_category_ids=None` 参数；仅在 `category_id is None`（全部）且列表非空时按权限过滤 |
| `get_shorts_for_random` | 新增 `allowed_category_ids=None` 参数；仅在 `category_id is None`（全部）且列表非空时按权限过滤 |

**语义约定**：`allowed_category_ids=None` → 不限制（旧行为兼容）；非空列表 → 仅这些分类内的视频；空列表 → 与 None 等价（全部可见）。

### 四、API 路由改动（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py)）

#### 4.1 `GET /api/v1/shorts`（L1711+）

- 新增逻辑：调用 `get_user_allowed_category_ids(uid, 'short')` 获取权限
- **权限校验**：用户有权限限制 + 请求的具体分类不在权限内 → 直接返回空列表 + `message: "无权访问该分类"`
- **exclude_played 分支**：传 `allowed_category_ids=allowed_ids` 给 `get_shorts_for_random`（仅 category_id=0 时生效）
- **普通查询分支**：传 `category_ids=allowed_ids` 给 `get_all_videos`（仅 category_id=0 时生效）

#### 4.2 `GET /api/v1/shorts/played`（L1657+）

- 新增逻辑：获取权限后传 `allowed_category_ids` 给 `get_short_played_status`
- 仅在 `category_id is None`（全部请求）时按权限过滤

#### 4.3 Web 端 `GET /shorts/play/<id>`（L281-293）

- 受限分支：`get_short_played_status(None, user_id=uid)` → `get_short_played_status(None, user_id=uid, allowed_category_ids=allowed_ids)`
- 这样「已播 X / Y」中的 Y 也会按权限显示正确的 1500


### 八、追加修复（2026-08-20 同日深夜）：`/api/v1/short_categories` 也按权限过滤

#### 8.1 问题复现

修复（一）到（七）部署后，APP 端反馈：登录用户 D（仅 A/B/C 权限）看到的「全部」卡片和播放界面**仍然显示 5000**。

#### 8.2 根因

APP 端的「短视频分类列表」界面是用 `GET /api/v1/short_categories` 接口拿 `total_short_count` 显示在「全部」卡片上的。原（一）到（七）的修复**没有覆盖**这个接口——它仍然返回全局 5000，所以 APP 端看到的还是 5000。

#### 8.3 修复（[app.py](file:///d:/文件管理/VIDEO/video_site/app.py#L1686-L1741)）

让 `/api/v1/short_categories` 接口在用户携带 Basic Auth 且有短视频分类权限限制时，按权限过滤三个字段：

| 字段 | 修复前 | 修复后（用户 D，仅 A/B/C 权限） |
|------|--------|-------------------------------|
| `categories` | 返回全部 10 个分类 | 仅返回权限内 3 个分类（含父子层级） |
| `category_counts` | 全部分类的视频数 | 仅权限内分类的视频数 |
| `total_short_count` | 全局 5000 | 权限内 1500 |

新增响应字段 `filtered_by_permission`（布尔），便于 APP 端调试时识别当前是否按权限过滤。

无 Basic Auth 或无权限限制的用户行为不变（保持全局返回）。

---

# 更新说明 - 2026-08-20（四）：新增「合集权限」开关

## 一、需求背景

原有用户权限管理仅对长视频分类（normal）和短视频分类（short）做精细控制，但合集（series）功能对所有登录用户开放，无法针对个别用户禁用。本次新增「合集权限」开关，让管理员可对单个用户关闭合集访问权限。

## 二、数据库变更

### 2.1 users 表新增字段

```sql
ALTER TABLE users ADD COLUMN can_access_series INTEGER DEFAULT 1;
```

- `1` = 可访问合集（默认，向后兼容现有用户）
- `0` = 不可访问合集

### 2.2 自动迁移

[models.py:136-145](file:///d:/文件管理/VIDEO/video_site/models.py#L136-L145) 新增 `_migrate_users_table(cursor)` 函数，在 `init_db()` 启动时自动为现有 users 表添加该字段，老数据库升级无需手动操作。

## 三、数据层改动 [models.py](file:///d:/文件管理/VIDEO/video_site/models.py)

### 3.1 新增函数


### 3.2 扩展 `get_user_permissions` 返回


## 四、API 路由改动 [app.py](file:///d:/文件管理/VIDEO/video_site/app.py)

### 4.1 APP 端合集接口增加权限校验

| 接口 | 改动 |
|------|------|
| `GET /api/v1/series` | 新增：`uid = _get_current_user_id()`；若 `get_user_series_access(uid) == False` → 返回 `403 {success:False, message:"无合集访问权限"}` |
| `GET /api/v1/series/<id>/episodes` | 同上：无权限返回 403（即使 series_id 不存在也先返回 403，避免泄露存在性） |

### 4.2 登录与权限查询接口自动返回 series 字段

无需额外改动，因为 `get_user_permissions` 已扩展。下列接口的 `permissions` 字段自动包含 `series: bool`：

- `POST /api/v1/login` → 响应 `permissions.series`
- `GET /api/v1/user/categories` → 响应 `permissions.series`
- `GET /api/users/<id>/permissions`（后台） → 响应 `permissions.series`

### 4.3 后台权限设置接口接受新字段


### 4.4 Web 端 series 详情路由增加权限校验

注：admin 角色豁免（管理员不应被自己的权限设置卡住）。

## 五、后台 UI 改动

权限设置弹窗「分类权限 - {username}」中新增「合集权限」区块：

加载状态：从 `/api/users/<id>/permissions` 响应读取 `permissions.series`，未传默认 `true`。

提交：在 PUT body 增加 `can_access_series: bool` 字段。

## 六、向后兼容性

- 现有 users 表通过 `_migrate_users_table` 自动添加 `can_access_series` 字段，默认值 `1`
- 现有用户默认有合集权限，行为与升级前一致
- PUT 接口未传 `can_access_series` 时保持默认 `True`，老 APP 客户端不受影响
- `_get_current_user_id()` 返回 None（未登录/未携带 Basic Auth）时，`get_user_series_access(None)` 返回 `True`，匿名访问行为不变


# 更新说明 - 2026-08-24：新增「数据库管理」功能

## 一、功能入口

在所有后台页面的侧边栏，「用户管理」下方新增「数据库管理」菜单项，页面路径 `/admin/db`，仅管理员可访问。

## 二、新增功能

### 2.1 数据库概览

- 显示数据库文件大小（MB）
- 显示各数据表行数统计：视频、系列、分类、短视频分类、用户、OpenList账号、用户分类权限、收藏、短视频播放标记

### 2.2 导出数据库备份

- 路由：`GET /admin/db/export`（管理员权限）
- 使用 SQLite backup API 生成一致性快照，导出过程不影响正在运行的服务
- 下载文件名格式：`videos_backup_YYYYMMDD_HHMMSS.db`

### 2.3 导入数据库备份（恢复）

- 路由：`POST /api/db/import`（管理员权限），multipart 表单上传
- 参数：
  - `db_file`：.db 备份文件（上限 512MB）
  - `include_users`：`1` = 同时导入用户信息（完整恢复），不传 = 仅恢复内容数据
- **完整恢复**（勾选导入用户信息）：备份数据整体覆盖当前数据库（含用户/权限/收藏/播放记录），完成后清空会话强制重新登录
- **仅恢复内容**（不勾选）：导入 videos、series、categories、short_categories、openlist_accounts 五张内容表，保留本机的 users、user_category_permissions、favorites、user_short_played
- 上传文件校验：SQLite 文件头魔数校验 + 核心表（videos/categories/users）存在性校验，防止误传无关文件
- 安全机制：每次导入前自动将当前数据库备份到 `data/import_backups/pre_import_时间戳.db`，导入失败可手动找回
- 兼容性：导入时按源表与目标表的列交集复制，兼容新旧版本备份的表结构差异；导入后自动执行 `init_db()` 迁移

## 三、其它改动

- `app.config['MAX_CONTENT_LENGTH']` 设为 512MB（原先未设置，无上限；现在限制上传体积防止滥用）
- `models.py` 新增 `get_db_stats()` 函数
- 新增模板 `templates/admin/db_management.html`
- 全部 12 个后台模板的侧边栏菜单统一增加「数据库管理」入口

# 更新说明 - 2026-08-24（二）：新增「跨设备数据库同步」功能

在「数据库管理」页面新增跨设备单向同步能力，将 A 设备的数据库推送到 B 设备。

## 一、功能总览

- 同步方向：**单向推送**（A → B 覆盖）
- 发送端（A）准备快照：导出全库 → **自动过滤本地导入视频**（`source='local'`，文件在 A 上，B 无法播放）→ **对 OpenList 播放/封面链接做批量查找替换** → 上传到 B
- 接收端（B）应用快照：内容表始终覆盖；用户信息/收藏/播放标记按**本机接收开关**决定
- 传输安全：HTTPS（项目 ProxyFix 已支持）+ 共享同步密钥（`X-Sync-Key` 头校验）

## 二、发送端（推送）设置

页面区块「跨设备同步 — 推送到远程设备」：

| 配置项 | 说明 |
|---|---|
| 目标服务器地址 | B 设备的 `http(s)://地址:端口` |
| 目标同步密钥 | B 设备的接收密钥 |
| OpenList 链接 — 查找 | 待替换的字符串（如 A 的服务器地址/端口/路径） |
| OpenList 链接 — 替换为 | 替换后的字符串（如 B 的服务器地址/端口/路径） |

- 查找/替换为空时不做链接替换；二者均作用于所有视频的 `video_url` 与 `cover`
- 配置持久化到 `data/sync_send_config.json`

## 三、接收端（接收）设置

页面区块「跨设备同步 — 接收设置」：

| 配置项 | 说明 |
|---|---|
| 同步接收密钥 | 本机用于校验推送请求的密钥（可随机生成） |
| 接收用户信息 | 覆盖本机 users + user_category_permissions |
| 接收用户收藏 | 覆盖 favorites |
| 接收短视频播放标记 | 覆盖 user_short_played |

- 视频/系列/分类/短视频分类/OpenList账号等内容表**始终同步**
- 未开启用户信息时，收藏和播放标记会**按用户名自动重映射**到本机用户（用户名不匹配或视频不存在的行自动跳过）
- 配置持久化到 `data/sync_receive_config.json`

## 四、新增接口

| 接口 | 方法 | 鉴权 | 说明 |
|---|---|---|---|
| `/api/sync/receive_config` | GET/POST | 管理员 | 读取/保存接收设置 |
| `/api/sync/send_config` | GET/POST | 管理员 | 读取/保存推送设置 |
| `/api/sync/gen_key` | GET | 管理员 | 生成随机同步密钥 |
| `/api/sync/push` | POST | 管理员 | 发送端：准备快照并推送到目标设备 |
| `/api/sync/receive` | POST | 同步密钥 | 接收端：接收并应用快照（非登录态，服务器间调用） |

## 五、安全与容错

- 接收端每次同步前自动将当前数据库备份到 `data/import_backups/pre_import_时间戳.db`
- 上传快照做 SQLite 魔数 + 核心表校验，防止误传
- 应用过程在单事务内进行，失败自动回滚，本机数据不受影响
- 推送超时 300 秒，连接失败/超时返回明确提示

# 更新说明 - 2026-08-24（三）：推送表单历史输入功能

「跨设备同步 — 推送到远程设备」中的 4 个输入框（目标服务器地址、目标同步密钥、OpenList 链接—查找、OpenList 链接—替换为）新增历史输入记录功能：

## 一、使用方式

- 每次点击「推送」时，本次填写的输入自动保存为历史记录（每字段独立保存）
- 输入框右侧新增时钟图标按钮，点击展开下拉菜单，列出该字段的历史输入，点击即填入输入框
- 历史记录自动去重、最新在最前，每字段最多保留 20 条
- 下拉菜单底部提供「清空该字段历史」入口

## 二、新增接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/sync/push_history` | GET | 获取各字段的历史输入记录 |
| `/api/sync/push_history?field=xxx` | DELETE | 清空指定字段的历史（不带 field 则清空全部） |

## 三、存储

历史记录持久化到 `data/sync_push_history.json`，与推送配置分开存储，清空历史不影响当前推送配置。

# 更新说明 - 2026-08-24（四）：EXE 打包

使用 PyInstaller 6.22.2 + `build_exe.spec` 构建单文件 EXE：

- 产物：`dist/视频管理系统.exe`（约 15MB），模板与静态资源已全部内嵌
- 双击运行：监听 `0.0.0.0:3090`，1.5 秒后自动打开浏览器 `http://127.0.0.1:3090`
- 首次运行在 EXE 同级目录自动创建 `data/videos.db`（默认账号 admin / password）
- 已冒烟测试：启动、登录页 HTTP 200、APP 登录 API 均正常

> 提示：EXE 模式数据（数据库/同步配置）保存在 EXE 同级 `data/` 目录，与开发目录互不影响；迁移部署时把整个 `data/` 目录随 EXE 一起拷贝即可。
