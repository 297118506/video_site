# 镜中景 · APP 功能介绍

一款运行于 Android 手机端的影视观看应用，必须配合服务端使用。包含首页影视库、沉浸式短视频、系列合集、个人收藏、图集五大板块。支持多服务器接入与多用户权限体系，不同账号拥有各自独立的观影范围和收藏空间。

---

## 一、首页（影视库）

- 浏览全部长视频影片，封面网格展示，包含分类标签、评分等信息。
- 支持下拉刷新与向上滚动自动加载更多内容。
- 点击影片进入详情页，可查看片名、分类、评分、年份、演员、播放量等信息并开始播放。

## 二、短视频

### 1. 短视频分类页

- 卡片式展示所有短视频分类，每张卡片显示该分类的视频数量。
- 「全部」卡片汇总当前账号有权查看的所有短视频，数量即为实际可看总数。
- 标题栏右侧提供随机模式开关（随机开/随机关），开启后进入播放将乱序呈现，每次刷新顺序都不同。
- 点击任意分类卡片（含"全部"）或空状态播放按钮即可进入对应视频流；选择多个分类时，视频流为所选分类内容的合并。
- 列表数据实时反映账号最新权限，管理员调整后无需重新登录。

### 2. 短视频播放页

采用全屏沉浸式设计，上下滑动即切换上一个/下一个视频：

| 手势 | 效果 |
|------|------|
| 上滑 | 切换到下一个视频 |
| 下滑 | 回看上一个视频 |
| 单击屏幕 | 暂停 / 继续播放 |
| 左右拖动 | 快退 / 快进（松手生效，屏显目标时间） |
| 长按屏幕不放 | 2 倍速播放，松手恢复正常速度 |
| 拖动进度条 | 精确定位到任意进度 |

播放界面上方实时显示：

- **播放进度**：已播 X / 总数 Y，计数跨会话保留（服务端持久化）。一个视频看完一轮自动从头再来，无需手动清理。
- **当前视频的分类名称与来源标识**（本地导入 / OpenList 导入）。

其它能力：

- 静音按钮一键静音/取消静音。
- 星标按钮收藏当前视频，即时写入收藏夹。
- 随机模式下加载下一批视频时新旧内容自动混排，避免重复观感。
- 视频 10 秒无法加载时自动跳到下一个；加载失败（文件丢失、源不可达等）同样自动切换，并根据来源给出相应提示。
- 支持蓝牙耳机 / 有线耳机的按键遥控（上下曲切换、暂停继续）。
- 应用切到后台自动暂停，回到前台自动继续。

## 三、合集

- 以系列为单位浏览连续内容，进入合集查看完整分集列表（集数、标题、封面、时长）。
- 点击任一分集直接从该集开始播放。
- 受合集权限控制的账号看不到无权访问的合集入口。

## 四、收藏

- 底部导航一键进入个人收藏列表，按收藏时间倒序排列。
- 与账号绑定：换设备登录同一账号收藏不丢，不同账号互相隔离。
- 在列表中点开即播，也可随时取消收藏。

## 五、图集

以图片为主、可混合视频的媒体合集板块，浏览体验类似手机相册。

### 1. 图集列表页

- 卡片式展示图集，每张卡片包含标题与两张并排缩略图预览（首张为封面，第二张自动懒加载）。
- 卡片底部显示媒体数量与浏览次数。
- 左上角「图集分类」入口进入分类页；向上滚动接近底部自动加载更多图集。
- 列表数据实时反映账号最新图集权限，管理员调整后无需重新登录。

### 2. 图集分类页

- 树形展示图集分类，顶级分类默认收起，点击箭头展开/收起子分类。
- 点击任意分类（含子分类）进入该分类的图集列表，可随时返回。

### 3. 图集浏览页

两级结构：先以网格总览全部内容，再进入灯箱模式逐个细看。

- **网格模式**：2 列平铺展示图集内全部媒体；图片直接显示缩略图，视频以播放图标与角标标识；顶栏显示图集标题与媒体总数。
- **灯箱模式**：点击任意媒体进入，全屏左右滑动逐个浏览，顶部实时显示「当前序号 / 总数」。
  - 图片按原始比例完整显示，横图竖图都不裁剪。
  - 视频滑动到当前页自动播放，单击画面暂停/继续，暂停时显示中央播放图标。
  - 视频底部控制条提供当前时间、时长与细进度条，进度条支持点击任意位置定位、拖动实时预览（松手生效）。
- 灯箱模式下按返回键或使用返回手势，先回到网格模式，不会直接退出图集。
- 图片仅使用内存缓存，APP 关闭后自动清除，不占用手机存储空间。

## 六、搜索

- 顶部搜索框输入关键词全局检索影片。
- 清空关键词自动清除搜索结果，无残留干扰。

## 七、账号与权限

- 使用服务端分配的用户名密码登录，登录状态持久保存。
- 权限体系支持按用户开放：可看的视频分类、是否可用短视频模块、是否可用合集模块。
- 管理员可在后台随时调整某用户的权限范围，APP 刷新列表即同步生效，无需退出重登。
- 无权访问的内容在列表中不展示；强行请求受限资源时会收到明确的「无权访问」提示。
- 更换服务器地址后自动退出登录，避免旧凭据误连新环境。

## 八、设置

- 自定义服务器根地址，同时兼容 http 与 https 协议，内网外网均可使用；填写示例格式一目了然。
- 地址历史记忆：保存过的服务器地址自动记录，通过输入框右侧的下拉菜单快速切换。
- 历史条目支持长按删除，管理更省心。
- 内置连接测试，保存前先验证服务器可达性。

---

*镜中景 Android 客户端 · 版本 4.2*
