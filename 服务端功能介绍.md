# 视频管理系统 — 功能介绍

> 一个可部署在个人电脑或局域网服务器上的视频管理与播放平台，支持网盘视频和本地视频两种来源，提供网页端和手机 APP 两种访问方式，可打包为单个 EXE 程序免安装运行。

---

## 一、系统总览

系统围绕三类内容组织：

| 模块 | 说明 |
|------|------|
| 普通视频 | 电影、剧集等长视频，支持分类浏览和详情页播放 |
| 短视频 | 独立分类体系，主打"随机播放、刷到没看过的"体验 |
| 系列合集 | 多集内容按合集归组，支持剧集顺序播放和自动连播 |

三类内容相互隔离，各自有独立的管理入口和分类，互不干扰。

系统支持**两种视频来源**：

- **OpenList 网盘视频**：连接 OpenList（或 AList）网盘服务，直接导入网盘里的视频，播放时走网盘直链，服务器不存储视频文件。
- **本地磁盘视频**：选择电脑上的文件夹导入视频，播放时由系统流式传输，**原始文件不会被移动、复制或修改**。

---

## 二、前台功能（WEB端）

### 2.1 视频浏览

- **首页瀑布流展示**：按分类分页展示视频卡片，显示封面、标题等信息。
- **分类导航**：顶部/侧边分类栏快速切换，支持多级分类（父分类→子分类）。
- **搜索**：顶部搜索框，按标题、描述等关键字查找视频。
- **播放量统计**：每次播放自动累计，显示视频热度。

### 2.2 视频播放

- **网页播放器**：支持 MP4 等常见格式，也支持 HLS(m3u8) 直播流格式。
- **视频详情页**：展示封面、标题、简介、年份、评分、演员、分类等信息。
- **合集播放**：进入合集后按集数顺序观看，播放器下方显示剧集列表。
- **自动连播**：播放完毕后自动播放下一集（可开关，并记住用户的选择），片尾有倒计时提示"即将播放下一集"。
- **跨设备访问**：同一局域网内的手机、平板、电脑都可以通过浏览器访问播放。

### 2.3 短视频中心

- **独立的短视频首页**：按短视频分类展示入口卡片，显示每个分类的视频数量。
- **随机播放模式**（核心体验）：
  - 进入分类后随机抽视频播放，上/下一个随机切换；
  - **自动跳过看过的**：已播放的视频不会再次出现；
  - **按用户独立记忆**：每个人看过的记录互相独立，A 看过的不影响 B 的随机列表；
  - **看完自动重置**：某分类全部看完后自动清空记录，从头开始新一轮随机播放。
- **播放标记管理**：后台可查看每个用户的观看进度，支持按用户清空或全部清空。

### 2.4 用户登录

- 访客可限制性浏览；登录后按权限看到对应内容。
- 每位用户有独立的收藏、播放记录、短视频观看进度。

---

## 三、后台管理

后台入口在网页左上角，仅管理员可进入，左侧菜单包含以下功能模块。

### 3.1 仪表盘

- 视频总数、短视频数、合集数、分类数、用户数等统计一览。
- 短视频播放进度表：每个用户"已播放/可看总数"及进度条，支持单用户/全部清空标记。

### 3.2 视频管理（普通视频 / 短视频）

- **列表管理**：分页浏览、按标题/描述搜索、查看详情。
- **手动添加/编辑**：填写标题、简介、封面、播放地址、分类、年份、评分、演员等。
- **批量操作**：批量删除、一键清空。
- **刷新 NFO**：对已导入的视频重新读取网盘中的 NFO 信息，更新标题、封面、简介等元数据。

### 3.3 分类管理

- **普通视频分类**：支持多级分类（父子层级）、排序、批量删除。
- **短视频分类**：独立于普通分类，单独管理，互不影响。

### 3.4 合集管理

- 创建/编辑合集，设置合集名称、封面、简介。
- 合集与视频关联，按集数排序展示。
- 支持批量删除与清空。

### 3.5 OpenList 网盘导入

- **网盘账号管理**：可添加多个 OpenList 账号，一键测试连接是否可用，支持内网/外网(HTTPS)网盘。
- **文件浏览器**：在网页里像资源管理器一样浏览网盘目录，支持前进/后退/上级目录。
- **文件预览**：点击眼睛图标即可预览网盘文件——支持视频在线播放（含 m3u8）、图片查看，其他文件提供下载。
- **四种导入方式**：
  - **单个导入**：选一个视频导入；
  - **多选导入**：勾选多个视频一次性导入；
  - **目录批量导入**：选一个文件夹，自动导入其中全部视频（可含子目录）；
  - **自动建合集**：按文件夹结构自动创建合集并归集剧集。
- **NFO 智能识别**：自动读取视频同目录的 NFO 信息文件，自动填入标题、简介、年份、评分、演员、封面等，无需手动填写。封面图片自动转换为可正常显示的网盘地址。
- **统一封面设置**：导入时可指定整个目录共用的封面。
- **导入进度可视化**：批量导入时实时显示"正在导入中，请稍候（视频较多时可能需要较长时间）10/100"式进度，已处理数/总数每秒刷新。

### 3.6 本地视频导入

- **本地文件夹浏览**：在网页中选择电脑上的目录，浏览其中的视频文件。
- **导入前预览**：查看视频信息、NFO 元数据、选择封面图片。
- **三种导入方式**：单个导入、多选导入、目录批量导入。
- **NFO 自动识别**：与网盘导入一致，自动读取本地 NFO 文件填充信息。
- **文件零打扰**：视频保持原位置不动，通过流式接口播放，不移动、不复制、不修改源文件。

### 3.7 用户管理

- 添加/删除用户、修改密码。
- **分类权限**：可为每个用户分别设置可见的视频分类和短视频分类；不设置则全部可见。
- **合集权限开关**：可单独控制某用户能否访问合集模块。

### 3.8 收藏管理

- 查看所有用户的收藏记录，支持搜索、批量删除、一键清空。

### 3.9 数据库管理

位于管理菜单最下方（用户管理之下），包含三大能力：

**（1）数据备份与恢复**

- **导出数据库**：一键下载完整数据库备份文件。
- **导入数据库**：上传备份文件恢复数据，导入时可选择**是否同时恢复备份里的用户信息**（不选则保留当前系统的用户）。

**（2）跨设备数据同步（A → B 单向推送）**

适用于"一台主力设备维护数据、多台设备接收"的场景：

- **接收端设置**（本机作为接收方）：设置同步密钥；可选择接收哪些内容——视频等内容数据、用户信息、用户收藏、短视频播放标记，四类各自独立开关。
- **发送端设置**（本机作为发送方）：
  - 填写目标服务器地址和同步密钥，一键生成密钥；
  - **本地导入视频自动过滤**：发送端本地电脑的视频同步过去也无法播放，同步时自动剔除；
  - **网盘链接自动替换**：可设置"查找/替换为"规则，自动把视频和封面地址里的网盘域名换成接收端使用的地址（例如 A 用 `123456` 端口路径，B 用 `7890`，自动完成转换）；
  - **历史输入记忆**：目标地址、密钥、查找、替换四个输入框都支持下拉选择历史输入记录（保留最近 20 条），重复配置不用重新输入。
- **接收端本地视频保护**：同步会覆盖接收端的视频数据，但接收端自己导入的本地视频会**自动保留**——同步后自动恢复、修正地址、保留相关的收藏和播放记录，不会丢失。

**（3）数据库概览**

- 显示数据库文件大小和各数据表的记录条数，随时掌握数据规模。

---


## 四、部署与运行

- **免安装 EXE**：整个系统打包为一个约 15MB 的 EXE 程序，windows系统双击即可运行，不需要安装 Python 或任何依赖环境。
- **飞牛os**：下载fpk安装包，手动安装。两个版本安装包：
- VideoSite-xx.xx.xx-pkg-fnos-all.fpk   "run-as": "package" ← 由应用中心自动分配普通系统身份，安全、可做 ACL 授权，使用“本地视频导入”时，无法浏览设备全部文件目录，权限限制比如vol1、vol2、vol00、vol01这些，部署在公网环境推荐安装这个版本。
- VideoSite-xx.xx.xx-root-fnos-all.fpk   "run-as": "root" ← 以 root 跑，能无差别访问任何路径，使用“本地视频导入”时，可以浏览设备全部文件目录，没有权限限制，部署在公网风险大，仅推荐在本地局域中环境中安装使用。
- **其他系统**：Linux、MAC完整克隆项目文件到本地，部署好Python环境，安装依赖后运行video_site文件夹中的“run.py”。
- **首次登陆**：服务端局域网内任意设备浏览器输入运行服务端的设备IP地址:3090访问WEB页面，如192.168.0.100:3090,默认用户名/密码为admin/password。

---

## 五、适用场景举例

| 场景 | 用法 |
|------|------|
| 个人影视库 | 网盘视频批量导入 + NFO 自动刮削，网页/APP 随处观看 |
| 家庭共享 | 一台电脑做服务器，家人各自账号，按年龄/喜好分配分类权限 |
| 短视频刷看 | 随机播放 + 跳过看过的，按人独立记忆，看完自动重置 |
| 多设备部署 | A 设备维护数据，一键同步到 B/C 设备，本地视频和用户数据各不冲突 |
| 定期备份 | 数据库一键导出备份，需要时导入恢复 |
